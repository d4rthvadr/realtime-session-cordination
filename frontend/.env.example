# Shared frontend runtime configuration (applies to both apps)
NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8080

# Used by admin app to build viewer links
NEXT_PUBLIC_USER_APP_URL=http://localhost:3001
